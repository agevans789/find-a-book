/* user profile is determined by the user's rating of each book
then the criteria of that book is taken as a percentage and added to the average of those criteria within the user's profile */

export const INITIAL_USER_PROFILE = {
    
}